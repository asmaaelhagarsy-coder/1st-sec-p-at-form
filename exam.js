export function initExam(root, examUrl){const host=document.createElement('div');host.className='card';host.innerHTML=`<p><strong>Instructions:</strong> Answer all questions. Do not refresh the page. When finished, click Submit.</p><button id="startExam">Start Exam</button><div id="examFrame" style="margin-top:12px"></div>`;root.innerHTML='';root.appendChild(host);host.querySelector('#startExam').addEventListener('click',async()=>{const res=await fetch(examUrl);const html=await res.text();host.querySelector('#examFrame').innerHTML=html;});}
